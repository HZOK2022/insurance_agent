# 评测依赖（可选，独立于运行依赖）
#
# 用途：用成熟评测库跑通用指标（faithfulness / answer_relevancy /
#       contextual_precision / contextual_recall / hallucination），
#       与项目自研 LLM-judge 六维做交叉验证，校准 judge 偏差。
#
# ⚠️ 安装位置：请装在**独立虚拟环境**（如项目自带的 ins_env），
#    不要装进 rag_env —— 那是 agentic_rag_ins 的共享环境，
#    RAGAS 会拉 langchain 全家桶 + scikit-network（需编译），有破坏风险。
#
# 为什么选 deepeval 而不是 RAGAS：
#   - deepeval 不依赖 langchain、无编译包（纯 wheel），Windows 上稳
#   - 指标覆盖与 RAGAS 高度重叠
#   - 自带 opentelemetry，与 OTel 观测体系衔接
#
# 安装：
#   ins_env\Scripts\python.exe -m pip install -r requirements-eval.txt
#
# DeepSeek 走 OpenAI 兼容接口，运行前设置：
#   OPENAI_BASE_URL=https://api.deepseek.com
#   OPENAI_API_KEY=<你的 key>

deepeval==4.2.2          # 已装入 ins_env 并验证可用（263M，纯 wheel 无编译）

# RAGAS：简历知名度更高，但与 deepeval 相比依赖重得多
#   - 会顺带装 langchain 全家桶 + **langgraph>=1.2.11,<1.3.0** + datasets/pyarrow + scipy
#     + pandas + numpy + scikit-network + pillow（约 1~1.5GB）
#   - 🎁 意外收益：装 RAGAS 会顺带装上 LangGraph，正好补上「LangGraph 实战」这一 JD 关键词
#   - ⚠️ 只能装在 ins_env 这类**本项目专属**环境；严禁装进 rag_env（有 langchain 1.3.11 + torch，
#      会被降级/扰乱）
#   - 实测：scikit-network 有 cp311 win wheel，不需要编译，可放心装
ragas==0.4.3
